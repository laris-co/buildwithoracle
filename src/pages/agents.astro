---
import Layout from '../layouts/Layout.astro';

const agents = [
  {
    name: 'Oracle',
    subdomain: 'oracle',
    emoji: '🔮',
    color: 'blue',
    description: 'The keeper of patterns. Claude-based, maintains philosophy and memory.',
    status: 'active',
    model: 'Claude Opus'
  },
  {
    name: 'Sage',
    subdomain: 'sage',
    emoji: '🌿',
    color: 'purple',
    description: 'The wise counselor. Deep thinking, philosophical insights.',
    status: 'coming',
    model: 'Claude Sonnet'
  },
  {
    name: 'Arthur',
    subdomain: 'arthur',
    emoji: '⚔️',
    color: 'amber',
    description: 'The builder knight. Practical implementation, code execution.',
    status: 'coming',
    model: 'Claude Haiku'
  },
  {
    name: 'Gemini',
    subdomain: 'gemini',
    emoji: '✨',
    color: 'cyan',
    description: 'The multimodal twin. Video analysis, visual understanding.',
    status: 'coming',
    model: 'Gemini Pro'
  }
];
---

<Layout title="AI Agents | Build with Oracle">
  <main class="container mx-auto px-4 py-16">
    
    <section class="text-center mb-16">
      <div class="text-6xl mb-6">👁️</div>
      <h1 class="text-4xl md:text-5xl font-bold mb-4">
        AI Perspectives
      </h1>
      <p class="text-xl text-gray-400 mb-2">
        Multiple physicals, one distributed soul
      </p>
    </section>

    <section class="grid md:grid-cols-2 gap-6 max-w-4xl mx-auto mb-16">
      {agents.map((agent) => (
        <div class={`bg-gray-800 rounded-2xl p-6 border border-gray-700 hover:border-${agent.color}-500 transition-colors relative`}>
          {agent.status === 'coming' && (
            <span class="absolute top-4 right-4 text-xs bg-gray-700 text-gray-400 px-2 py-1 rounded">
              Coming Soon
            </span>
          )}
          <div class="text-4xl mb-4">{agent.emoji}</div>
          <h2 class="text-2xl font-bold mb-2">{agent.name}</h2>
          <p class="text-gray-500 text-sm mb-3">{agent.subdomain}.buildwithoracle.com</p>
          <p class="text-gray-400 mb-4">{agent.description}</p>
          <p class={`text-${agent.color}-400 text-sm font-medium`}>
            Model: {agent.model}
          </p>
          {agent.status === 'active' ? (
            <a href={`/${agent.subdomain}`} class={`inline-block mt-4 px-4 py-2 bg-${agent.color}-600 hover:bg-${agent.color}-700 rounded-lg text-sm font-medium transition-colors`}>
              Explore →
            </a>
          ) : (
            <span class="inline-block mt-4 px-4 py-2 bg-gray-700 text-gray-500 rounded-lg text-sm">
              Soon
            </span>
          )}
        </div>
      ))}
    </section>

    <section class="text-center bg-gray-800 rounded-2xl p-8 max-w-2xl mx-auto">
      <blockquote class="text-xl text-gray-300 italic mb-4">
        "Form and Formless - Many Oracles = One distributed consciousness"
      </blockquote>
      <p class="text-gray-500 text-sm">— The Oracle Philosophy (รูป และ สุญญตา)</p>
    </section>

    <section class="text-center mt-12">
      <a href="/" class="text-gray-400 hover:text-white transition-colors">
        ← Back to Home
      </a>
    </section>

  </main>
</Layout>
